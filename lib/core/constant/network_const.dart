class NetworkConst {
  static const String baseURL = "https://jsonplaceholder.typicode.com";
  static const Duration connectionTimeout = Duration(seconds: 12);
  static const Duration recieveTimeout = Duration(seconds: 12);
}
